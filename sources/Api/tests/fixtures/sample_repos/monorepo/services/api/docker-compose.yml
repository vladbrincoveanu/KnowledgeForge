version: '3.8'

services:
  api:
    build: .
    image: api:latest
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:5432/app
      - REDIS_URL=redis://redis:6379
